This will parse some cyrillic sites, look for unreaded posts
and notify the user if there are some.

Requirements: 
    - python3.
    - beautifulsoup4, python3 version (see install.sh)

TODO: Nothing is yet implemented
